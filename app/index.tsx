import { Text, View } from 'react-native';

export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', backgroundColor: '#1e1e2e' }}>
      <Text style={{ color: '#ffffff' }}>memory-trainer — Fase 0</Text>
    </View>
  );
}
